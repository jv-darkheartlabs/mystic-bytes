# Local dev + demo walkthrough

1. Run the site locally:
   - `bundle install`
   - `bundle exec jekyll serve`
2. Confirm key pages render and navigation works.
3. Optional: add a small screenshot/gif for the repo README to show the site at a glance.
