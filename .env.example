# Copy to .env in this project root (never commit .env)
#
# Tetrate Agent Router — drop-in OpenAI-compatible endpoint
OPENAI_API_KEY=your-tetrate-key-here
OPENAI_BASE_URL=https://api.router.tetrate.ai/v1

# Optional: override model in generate-book-club-reviews.mjs via --model
